# Neon PostgreSQL
DATABASE_URL="postgresql://user:password@host.neon.tech/neondb?sslmode=require"

# MongoDB - Historial DIAN, auditoría, IA, snapshots.
# Producción (Cloud Run): inyectar URI vía Secret Manager como MONGO_URI (o MONGODB_URI). No usar .env.
# Desarrollo local (sin Atlas): MONGODB_URI=mongodb://localhost:27017/mottatech
#   Ejecutar MongoDB: docker run -d -p 27017:27017 mongo:7
# Atlas: agregar tu IP en Network Access (Atlas → Cluster → Network Access → Add IP).
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
MONGODB_URI=mongodb://localhost:27017/mottatech

# Sin MongoDB (sin Docker/Atlas): la app arranca pero sin DIAN, POS, Audit, IA, Analytics, Snapshots.
# SKIP_MONGODB=1

# JWT (producción: mínimo 32 caracteres aleatorios; TLS 1.3 recomendado en tránsito)
JWT_SECRET=your-super-secret-key-min-32-chars
JWT_EXPIRES_IN=7d

# DIAN (ambiente pruebas / producción)
DIAN_ENV=habilitacion
DIAN_API_URL=https://vpfe-hab.dian.gov.co
DIAN_CLIENT_ID=
DIAN_CLIENT_SECRET=

# Certificado digital (ruta o base64 cifrado)
DIAN_CERT_PATH=
DIAN_CERT_PASSWORD=

# Mercado Pago (pasarela de pagos)
MERCADOPAGO_ACCESS_TOKEN=APP_USR-95756689615799-020413-dcf8c73d514eeea65171f6cfef24e348-1426637362
MERCADOPAGO_PUBLIC_KEY=APP_USR-5852a4e0-1d92-4c65-90a3-bc0ef57bc8b6
# Opcional: webhook secret para validar notificaciones (crear en panel MP)
MERCADOPAGO_WEBHOOK_SECRET=

# Copilot IA - Opción B: Gemini para preguntas sin datos en BD (opcional)
# Obtener key en https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=

# Modo Developer: API keys para sitios/apps externos que facturan ELC
# Se generan desde el dashboard; no poner keys aquí, se guardan cifradas en DB
# DEVELOPER_API_KEYS_ENABLED=true

# CORS (producción: dominios concretos, ej. https://app.myrsmartinvoicing.com)
CORS_ORIGIN=*

# App
PORT=8080
NODE_ENV=development
# URLs para callbacks de pago y webhooks (producción: Cloud Run o tu dominio)
APP_URL=http://localhost:3000
# Producción (Cloud Run): https://smarinvoicing-859618564124.us-central1.run.app
API_URL=http://localhost:8080